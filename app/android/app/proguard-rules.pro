# Additional app-specific R8 rules can live here if a plugin or reflection-heavy
# integration needs to keep classes from being removed. The default optimized
# Flutter setup is usually sufficient, so this file intentionally starts empty.
